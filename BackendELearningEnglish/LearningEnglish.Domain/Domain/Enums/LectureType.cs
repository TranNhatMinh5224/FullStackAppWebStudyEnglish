namespace LearningEnglish.Domain.Enums;

public enum LectureType
{
    Content = 1,      // Bài đọc văn bản (Markdown/HTML)
    Document = 2,     // Tài liệu đính kèm (PDF, Docx...)
    Video = 3         // Bài giảng video
}
